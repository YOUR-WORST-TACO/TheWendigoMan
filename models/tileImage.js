class TileImage {
    constructor(size, height, width, mimetype, fileName, fullPath) {
        this.size = size;
        this.height = height;
        this.width = width;
        this.mimetype = mimetype;
        this.fileName = fileName;
        this.fullPath = fullPath;
    }
}